pipeline {
    agent any
    
    stages {
        stage('Deploy') {
            steps {
                // complete this code to deploy to real k8s cluster
                echo change in YAML the value docker image to: $ROBERTA_IMAGE_URL
                echo 'echo kubectl apply -f ....'
            }
        }
    }
}