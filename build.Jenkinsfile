pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls'
                sh 'echo building...'
                sh 'echo checking build pipeline'
                sh 'echo checking build pipeline'
            }
        }
    }
}